[package]
name = "choamc"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "choamc"
path = "src/lib.rs"

[[bin]]
name = "choamc"
path = "src/main.rs"

[dependencies]
catalog = { path = "../catalog" }
engine = { path = "../engine" }
row = { path = "../row" }
sql = { path = "../sql" }

[dev-dependencies]
tempfile = "3"
